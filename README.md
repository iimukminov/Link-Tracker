# Link Tracker (Bot & Scrapper)

Система мониторинга обновлений в **GitHub** (Issues, PR) и **StackOverflow** (ответы, комментарии) с автоматическим уведомлением пользователя через Telegram-бота.

## 🚀 Быстрый запуск

Проект полностью подготовлен к работе в Docker-контейнерах. Вся инфраструктура, включая базу данных и микросервисы, настраивается через единый файл окружения.

### 1. Настройка окружения

Создайте файл `.env` в корневом каталоге проекта, скопировав содержимое шаблона `env_dist`:

```bash
cp env_dist .env
```

В файле `.env` необходимо заполнить следующие параметры:
* **Telegram**: `TELEGRAM_BOT_TOKEN` — токен вашего бота от [@BotFather](https://t.me/BotFather).
* **GitHub**: `GITHUB_TOKEN` — Personal Access Token для работы с API.
* **StackOverflow**: `STACKOVERFLOW_KEY` и `STACKOVERFLOW_ACCESS_KEY` — ключи доступа к API.
* **Database**: По умолчанию установлены стандартные значения (`postgres/postgres`), которых достаточно для локального запуска.

### 2. Запуск системы

Запустите все компоненты одной командой:

```bash
docker compose up -d
```

Docker Compose автоматически выполнит:
1. Поднятие контейнера **PostgreSQL**.
2. Применение миграций **Liquibase** для создания схем и индексов.
3. Сборку и запуск микросервисов **Bot** и **Scrapper**.

---

## 🛠 Особенности реализации

* **Параллельная обработка**: Scrapper использует многопоточный пул для одновременной проверки батчей ссылок (количество потоков настраивается).
* **Изоляция сбоев**: Ошибка при проверке одной ссылки не блокирует работу планировщика. Бот отправляет пользователю отчет, если ресурс временно недоступен.
* **Гибкость данных**: Поддержка двух режимов доступа к БД через параметр `APP_DATABASE_ACCESS_TYPE`:
  * `SQL` — прямое взаимодействие через JDBC.
  * `ORM` — использование JPA (Hibernate).
* **Сетевое взаимодействие**: Внутри Docker-сети сервисы общаются по именам (`http://scrapper:8081` и `http://bot:8080`), что исключает конфликты портов на хосте.

--- 

## Пояснение выбора настроек топика

Я выбрал 3 партиции для параллельной обработки и 3 реплики, так как мой кластер в docker-compose состоит из 3-х брокеров. Это гарантирует, что данные не пропадут, даже если два сервера выйдут из строя

---

## Отчет о нагрузочном тестировании (Производительность и Кэширование)

В рамках оптимизации Scrapper API было реализовано двухуровневое кэширование запросов (`GET /list`):
* **L1 (Client-Side Cache):** Внутрипамятный кэш на базе `Caffeine`.
* **L2 (Distributed Cache):** Распределенный отказоустойчивый кэш на базе кластера `Valkey` (3 ноды).

### Конфигурация тест-кейса (k6)

* **Объем данных в БД:** 100 000 ссылок.
* **Пользователи (VUs):** 20 параллельных потоков.
* **Сценарий:** 1% операций записи (`POST`), 99% операций чтения (`GET`).
* **Длительность:** 1 минута разгона (ramp-up) + 5 минут удержания нагрузки.

### Результаты замеров

| Конфигурация                    | Запрос   | RPS      | Среднее время | p(50)       | p(95)        | HTTP 200 | Ошибки |
|:--------------------------------|:---------|:---------|:--------------|:------------|:-------------|:---------|:-------|
| **Без кэша (PostgreSQL)**       | GET/POST | ~125     | 40.91 мс      | 40.74 мс    | 64.46 мс     | 100%     | 0      |
| **L2 кэш (Valkey Cluster)**     | GET/POST | ~165     | 6.08 мс       | 4.87 мс     | 12.48 мс     | 100%     | 0      |
| **L1 + L2 (Caffeine + Valkey)** | GET/POST | **~169** | **4.40 мс**   | **2.91 мс** | **11.82 мс** | 100%     | 0      |

### Выводы

1. **Снижение нагрузки на БД:** Использование Valkey (L2) позволило полностью снять нагрузку на чтение с PostgreSQL для часто запрашиваемых данных, что привело к снижению среднего времени ответа более чем в **6.5 раз** (с ~41 мс до ~6 мс).
2. **Устранение сетевых задержек:** Дополнительное включение Client-Side Caching (L1) позволило сократить медианную задержку p(50) еще на **40%** (до 2.91 мс). Локальный кэш позволяет обрабатывать данные мгновенно, а базовый overhead в 3-4 мс обусловлен исключительно работой веб-сервера Tomcat и сериализацией JSON.
3. **Консистентность данных:** При выполнении операций `POST` и `DELETE` (1% запросов) кэш корректно инвалидируется на обоих уровнях одновременно, гарантируя актуальность выдаваемого списка отслеживаемых ссылок.

