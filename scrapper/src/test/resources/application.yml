spring:
  liquibase:
    enabled: true
    change-log: classpath:migrations/master.xml
  jpa:
    hibernate:
      ddl-auto: validate
  data:
    redis:
      cluster: null

app:
  github:
    token: mock-token
  stackoverflow:
    key: mock-key
    access-token: mock-token
  scheduler:
    enable: false
